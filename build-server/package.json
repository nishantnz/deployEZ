{
  "name": "build-server",
  "version": "1.0.0",
  "description": "does the job of building git project adn uplaoding to S3",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NZ",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.556.0"
  }
}
